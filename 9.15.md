# AIPQ 项目工作日志

## 今日目标

继续处理暂停恢复、录音录像完整性、补答流程和管理员展示问题，重点核查近期出现的音频过低、视频不完整、答案状态不一致及多次面试缺失题重复显示问题。

---

## 1. 暂停恢复与补答功能

### 问题

ID110 暂停后恢复时，系统错误沿用了暂停前的旧题目 deadline，导致学生恢复后很快被判定超时，没有获得完整的 180 秒答题时间。

### 根因

恢复流程清除了服务端 deadline，但前端随后调用 `save_progress` 时又把浏览器内存中的旧 deadline 写回数据库。服务端后续检查旧 deadline，错误地将原题判定为超时。

### 修复内容

- 恢复后由服务端重新设置题目 deadline；
- 恢复固定回到暂停时的未完成题；
- 恢复后重新获得完整 180 秒；
- `save_progress` 不再覆盖服务端权威 deadline、`answer_started_at` 或超时状态；
- 重复恢复和重复 `question_state` 保持幂等；
- 增加补答任务模型，补答 Session 与原 Session 独立关联；
- 补答只允许访问指定缺失题，不会覆盖原有答案和媒体。

### 迁移演练

在只读备份副本中完成 migration 演练：

- 原有 Session、答案和媒体数据保持一致；
- migration 可重复执行；
- 模拟中断后重新启动可以恢复；
- SQLite integrity check 通过；
- foreign key check 为 0。

生产数据库尚未执行 migration。

---

## 2. 部署门禁修正

### 问题

生产库中存在大量 `completion_status='in_progress'` 的准备状态 Session，旧门禁将这些空 Session 统一视为正式答题，导致部署一直被阻断。

### 修复

重新区分：

- 正式开始答题；
- 页面准备但尚未开始答题；
- 有效录制 lease；
- 活跃上传；
- 转码、合并、导出和 TTS 任务。

门禁现在只把存在题目级开始、deadline、答案或录制证据的 Session 视为正式答题阻断。

### 验证

- 门禁专项测试通过；
- 67 条历史 `in_progress` Session 完成分类；
- 正式答题数量可与准备状态分开统计；
- 数据库读取前后 SHA-256 一致，门禁本身没有写入生产数据。

后续部署仍需等待正式答题、lease 和上传任务全部归零。

---

## 3. 音频质量检测与部署

### 问题

部分 Session 的音频轨道存在，但音量极低，管理员页面容易错误显示为"录音无"。

### 修复

新增音频质量分类：

- `audio_present`
- `audio_low_level`
- `audio_silent`
- `audio_upload_missing`

调整麦克风预检：

- 使用连续采样窗口；
- 短暂峰值但大部分静音时要求重新说话；
- 连续低音量语音允许通过，但标记为低电平；
- 答题期间自然静音不触发录制故障。

### 生产部署

已部署音频质量版本：

- 静态版本：`20260915-audio-quality-1`
- 已重启 `aipq-web.service`
- 已重启 `aipq-worker.service`
- Nginx 未重启
- Web、Worker、Nginx 均为 active
- 启动日志无异常

部署前完整测试：

- `802 passed`
- `compileall` 通过；
- JavaScript 语法检查通过；
- `git diff --check` 通过。

---

## 4. ID279 音频问题核验

ID279 最新 Session：

- 状态：`interrupted`
- 进度：`0/15`
- Session 时长：约 32 分 24 秒；
- 实际录制约 2 分 33 秒；
- 随后暂停 30 分钟并超时中断。

核验结论：

- 音频轨道存在；
- 原始 WebM、MP4、M4A 均可完整解码；
- 平均音量约 `-42.8 dB`；
- 属于音频电平过低，不是音频轨道缺失；
- 没有答案上传、答案文件或 media checkpoint；
- 不能回补为已回答，需要重新答题。

同时发现页面显示"录音无"与 manifest 的 `audio_low_level` 分类不一致，后续需要修复管理员展示逻辑。

---

## 5. ID64 音频和答案状态核验

ID64 最新 Session：

- 进度显示 `14/15`；
- 两个录制分段均完整；
- 两个转码任务均成功；
- MP4、M4A 和 15 个逐题 MP3 均存在并可解码；
- 音频轨道正常，未发现转码丢声；
- 曾出现短暂上传中断，但之后分片全部补齐。

主要问题是 `ability2__3` 的状态没有完全同步：

- 数据库题目状态为 `answered=1`；
- MP3 和 media checkpoint 存在；
- timeline 存在开始和完成事件；
- 但 `standard.json` 缺少该答案；
- `progress` 仍为 `14/15`；
- `unsubmitted_answers` 仍保留旧的中断标记。

结论：

- ID64 不需要重新答题；
- 需要后续进行有备份、有审计、幂等的状态回补；
- 本轮未修改生产数据。

---

## 6. 管理员题目时间显示问题

### 问题

部分题目显示"答案已保存，媒体已确认"，但开始时间、结束时间、用时和结束原因显示为"暂无记录"。

### 根因

答案和媒体回补流程只同步了答案状态，没有同步题目时间字段。时间线中实际上存在可靠的：

- `question_presented`
- `answer_started`
- `answer_finished`

### 修复

- 从服务端 timeline 提取题目时间；
- 回补答案和 checkpoint 时同步题目开始、结束和用时；
- 使用 `COALESCE` 保留已有权威时间；
- 缺少可靠数据时显示"时间未记录"，不伪造时间；
- 管理员页面逐字段显示真实值。

验证结果：

- 完整 pytest：`807 passed`
- `compileall` 通过；
- JavaScript 语法检查通过；
- `git diff --check` 通过。

该修复目前只完成代码和隔离测试，尚未部署，ID64 历史数据也尚未回补。

---

## 7. 补答缺失题跨历史 Session 显示问题

### 问题

同一学生存在多次面试时，管理员"补答缺失题"列表把多个历史 Session 的缺失题合并显示，导致同一组题目重复出现。

### 根因

候选题查询遍历了该学生的所有终态 Session，没有真正按当前选中的 Session 过滤。

### 修复方案

- 默认只读取最新 Session；
- 支持显式指定 `session_id`；
- 缺失题唯一键使用 `(session_id, question_key)`；
- 补答任务继续绑定 `original_session_id`；
- 旧 Session 单独打开时只显示旧 Session 自己的缺失题。

本轮已完成根因定位和修复方案设计，尚未完成生产部署。

---

## 8. 补答视频插入原视频的设计确认

确认当前补答任务即使能完成答案和媒体回补，也不能默认认为补答视频已经插入原始全程 MP4。

后续新补答功能应采用：

- 不覆盖原始 MP4/M4A；
- 保存独立的补答视频和音频；
- 根据原 Session 的题目 checkpoint 确定插入位置；
- 按题目顺序重新编码生成版本化完整媒体；
- 合并成功后才切换管理员默认播放版本；
- 合并失败时保留原媒体和补答媒体；
- 重复执行不得重复插入。

历史 Session 暂不处理，先保证新补答流程正确。

---

## 9. 待继续审查的 Session

已安排只读审计：

- ID101；
- ID181。

重点确认：

- 三个录制分段是否都完整；
- 当前页面是否只是显示最近一个分段；
- 是否缺少 Session 级合并视频；
- 哪一道题导致 `14/15`；
- 是否存在分片、转码、答案登记或媒体 checkpoint 问题；
- 是否可以通过合并或状态回补解决，而不要求学生重新答全部题。

目前尚未对 ID101、ID181 执行生产写操作。

---

## 10. 当前状态与下一步

### 已完成

- 暂停恢复 deadline 根因定位和代码修复；
- 补答数据模型和 migration 演练；
- 音频质量分类和生产部署；
- ID279、ID64 音频链路只读审计；
- 管理员题目时间显示问题定位和代码修复；
- 多历史 Session 缺失题重复显示问题定位。

### 尚未完成

- ID64 状态一致性回补；
- ID101、ID181 只读审计；
- `audio_low_level` 管理员显示修正；
- 缺失题列表按最新 Session 过滤的部署；
- 新补答视频按题目顺序插入完整 MP4 的实现和隔离测试；
- 暂停恢复和补答功能的生产部署。


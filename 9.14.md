# AIPQ 项目工作日志 — 2026-09-10 至 2026-09-15

## 工作范围

9 月 9 日完成基础生命周期、看板、暂停接管、Live2D、切屏和基础媒体处理后，继续处理生产环境中发现的并发保护、录音录像可靠性、转码恢复、暂停恢复计时、答案与媒体确认、补答和部署门禁问题。

本阶段多次严格区分：

- 只读核验：只读取生产数据库、日志、manifest 和媒体，不写入生产；
- 临时恢复：仅在 `/tmp` 临时目录重建或转码，不覆盖正式文件；
- 代码与隔离测试：只修改代码和测试，不连接生产数据库；
- 正式发布/部署：先备份、完整性检查和门禁确认，再复制版本化媒体或重启服务。

---

## 1. 生产部署安全门禁和 Session 状态核对

### 问题

早期部署检查直接按 `completion_status='in_progress'` 统计，导致大量登录后尚未开始答题的预开始 Session 被误认为正在答题，曾出现 42、45 甚至 67 条 `in_progress` 阻断部署的情况。

### 排查结果

逐条检查后确认，很多记录只有 OAuth 登录、准备页心跳或设备准备，没有第一题 deadline、题目开始时间、答案、录音、有效 lease 或上传活动。真正应阻断部署的是正式答题、有效录制 lease、活跃上传和运行中的媒体任务。

### 修复

调整 `scripts/preflight_smoke.py` 的门禁口径：

- 正式答题必须有题目级 `started_at/deadline_at/answered/finished` 证据；
- 有效 lease、活跃上传、转码、合并、导出、TTS 和补答任务独立阻断；
- `created`、`preparing`、`not_started` 和无正式题目证据的空 Session 归类为 `pre_start_active`，不计入正式答题；
- 门禁读取保持 SQLite 只读，不批量修改历史 Session。

### 验证

最终预检曾确认：

- `formally_started_active=0`；
- 有效 lease、活跃上传、转码、合并、导出、TTS 和补答活动均为 0；
- 部署阻断对象为 0；
- SQLite 完整性和外键检查通过。

但后续又发现 2 个预开始页面持续发送心跳。它们不属于正式答题，但重启 Web 仍可能影响准备页面，因此最新一轮部署暂缓，未强行清理 Session。

---

## 2. 正式答题并发和 FFmpeg 资源保护

### 问题

此前虽然做过 50 路并发测试，但生产代码没有明确设置正式答题人数上限，容易在几十人同时进入时造成 Web、SQLite、内存和媒体处理资源压力。

### 修复

增加正式答题容量保护：

- `AIPQ_MAX_ACTIVE_SESSIONS=8`；
- 第 9 个正式开始请求返回 HTTP 429；
- 错误码为 `active_session_capacity_reached`，并返回 `Retry-After: 30`；
- 容量拒绝不会写入 `started_at`、第一题 deadline 或录制 lease；
- 使用 `BEGIN IMMEDIATE` 处理多 Worker 并发竞争；
- paused、preparing、预开始和终态 Session 不占正式答题名额；
- 暂停恢复时重新申请容量。

视频处理同时限制为：

- 全局最多 1 个 FFmpeg 转码/合并进程；
- 单个 FFmpeg 最多 2 个编码线程；
- 转码和合并共用数据库队列槽与跨进程锁；
- 成功输出复用，失败任务和原始媒体保留。

### 验证

隔离并发测试覆盖 8、20、50 个正式开始请求，正式答题数未超过 8；容量专项和完整回归均通过。生产配置后来确认 `AIPQ_MAX_ACTIVE_SESSIONS=8`、`AIPQ_TRANSCODE_WORKERS=1`、`AIPQ_TRANSCODE_THREADS=2`。

---

## 3. 录音录像设备检测和答题期间连续性监测

### 背景问题

ID312 暴露出：Safari 15.1 摄像头预览正常，但 Session 录像使用不兼容的 WebM，最终没有视频分片，逐题音频也接近全程静音。此前只检查轨道存在和 MediaRecorder 能启动，不能证明实际画面和声音有效。

### 修复

新增和调整：

- Safari 优先使用 MP4/H.264/AAC，Chrome/Edge 优先使用 WebM/VP8/Opus；
- 开始前检查最终 MediaRecorder 流的音视频轨道、MIME、MediaRecorder 启动和首个有效分片；
- 麦克风预检要求用户正常说话约 3 秒，但答题期间不以人声有无判断故障；
- 答题期间每 2 秒本地检查录制技术链路，每 10 秒上报有界健康摘要；
- 连续 10 分钟没有有效音频或视频数据时分别提示学生和管理员；
- 区分轨道故障、MediaRecorder 不产片、分片上传失败和服务端未落盘；
- 静音、听 TTS、阅读和思考不会触发录音故障；
- 告警只记录一次，恢复也只记录一次，不自动结束 Session、不删除答案、不增加主动暂停次数；
- 前置摄像头检查增加 Canvas 多帧采样，识别纯黑、无信号静态图标和完全冻结画面；
- 暗光但有纹理和变化的画面只提示或允许通过，不做过严阻断；
- 不做人脸识别，不上传截图，不以音量、人声或亮度作为答题期间故障条件。

### 测试

覆盖了正常人物画面、暗光画面、纯黑画面、静态摄像头关闭图标、视频冻结、音频缺失、上传失败、TTS/静音和恢复等场景。测试环境曾发现 `admin_runtime` fixture 缺失，已修复为每用例独立临时数据库和媒体目录，完整测试最终达到 `776 passed`。

### 部署状态

录音质量、Safari MIME fallback、连续性监测和画面采样代码已完成隔离测试；部分前端录制修复曾在安全窗口部署并完成静态资源校验。Safari 15.1 仍需要真实设备新建 Session 验收，不能仅凭自动化测试宣称完全兼容。

---

## 4. 摄像头无有效画面的历史问题

### ID312

Safari 15.1 录像初始化失败，Session 没有有效视频分片，音频也接近全程静音。该媒体无法通过转码恢复，问题归因于浏览器 MediaRecorder 兼容性；已加入 MP4 fallback 和开始前录制能力检查。

### ID261

Windows Edge 152，音视频分片完整、转码成功，但原始视频从第一帧开始就是极暗、近乎静止的画面。原始 WebM 和正式 MP4 五个采样点一致，没有人物或场景纹理。不是 Safari、FFmpeg、播放器或分片问题，最可能是镜头被遮挡、选错摄像头、虚拟摄像头或设备输出无信号。现有像素无法恢复，必须重新录制。

### ID264

Windows Chrome 152，228 个分片完整，音视频轨道和转码均正常；原始视频从开始就是静态黑底白色“摄像头关闭/无信号”图标。原始媒体和 MP4 完全一致，不能通过重新转码恢复人物画面。该案例暴露出旧代码只检查轨道状态和非空 Blob，未真正调用 `videoQualityWarning()` 做像素采样。

### 后续修复

已把实际视频帧采样接入前置检测，并增加答题期间长期冻结/无信号告警。纯黑、静态无信号和完全冻结会阻止正式开始；暗光但有真实纹理和变化的画面不应被误杀。

---

## 5. 历史媒体恢复与转码问题

### ID266

ID266 完成 15/15，256 个分片 `0..255` 连续，大小和 SHA-256 全部匹配。第 2 段仅因浏览器离开未正常封口，原始 VP8/Opus 仍可完整解码。通过临时目录重新编码为 H.264/AAC 后，与第 1、3 段使用 concat 滤镜重新编码合并，PTS 和 time base 正常。

恢复版已使用版本化名称发布，原始分片、manifest、原 MP4/M4A 和审计记录均保留。中断点附近可能少量丢失未封口帧，但不需要重新答题。

### ID14

ID14 完成 15/15，7 个分片完整并包含音视频，但录制合同未封口，实际媒体约 60 秒。临时转码恢复成功，已发布为 `partial_recovered / recording_incomplete`，不能标记为完整视频。无法补回本来没有录制的后续时段；若业务必须要完整录像，需要重新开 Session。

### ID249

ID249 完成 15/15，261 个分片完整，原始媒体时长约 `2612.472s`。旧代码按 manifest 预计时长的 120% 上限校验，因预计时长偏小而错误判定 `normalized_stream_invalid`，编码实际上尚未开始。

修复后保留明显截短和绝对最大时长检查，允许合理的长录像，并统一重置 PTS/time base。ID249 已生成并发布版本化 H.264/AAC 视频和音频，播放、下载、ffprobe 和完整解码均通过；原失败任务仍保留为历史审计，不伪装成普通成功任务。

### 看板状态修复

看板现在以经过验证的当前恢复媒体为准：

- `recovered_versioned` 且完整覆盖预期分段：显示完整视频已就绪，可附带“恢复版”；
- `partial_recovered`：显示部分恢复、录制不完整；
- 没有有效恢复输出时才显示历史转码失败；
- 原 `session_transcode_jobs.failed` 仅作为审计记录，不覆盖当前有效恢复媒体。

---

## 6. 答案与媒体保存确认

### 问题

学生完成一道题时，答案请求可能先到达，而最后一个音视频分片仍在浏览器队列或尚未落盘。旧流程可能先把题目计为完成，再留下未确认媒体。

### 修复

实现题目级媒体 checkpoint：

1. 提交答案前调用 `MediaRecorder.requestData()`；
2. 等待当前分片生成和上传队列排空；
3. 上报最高连续分片序号；
4. 服务端在录制锁内核对分片已落盘后，才将题目标记为 `answered`；
5. 媒体未确认时保留答案，但不推进下一题；
6. 重复提交复用已保存答案，不重复计题；
7. 管理员显示“答案已保存，媒体已确认/待确认/保存失败”。

后续又补充了分片成功和 finalize 后自动回补 pending 答案、`finishing/media_pending` 不被超时扫描提前终结，以及跨 Worker 原子写入和并发幂等测试。

### ID184

ID184 页面曾显示 `3/15`，但逐题检查发现 15 个答案文件均存在，其中 12 个处于旧流程留下的 pending 状态；最终 146 个分片完整覆盖这些题目的媒体范围，整段音视频可解码。根因是旧客户端未携带有效 checkpoint 合同，后端没有在分片上传完成或 finalize 后批量回补 pending 答案，finish 又只统计已正式计入的 3 题。

该问题已经加入自动回补逻辑和回归测试，但当时尚未修改生产 ID184 状态。正式操作前仍需备份并审计确认，不能直接凭页面行数把 Session 改为完成。

---

## 7. 暂停恢复计时 Bug 和补答功能

### ID110 问题

ID110 最终为 `14/15 interrupted`，缺失第 1 题。审计确认：

- 第 1 题没有答案请求、答案文件、pending 答案或逐题音频；
- 自动断线暂停时保存剩余 `177 秒`；
- 恢复后虽然回到了原第 1 题，但前端 `save_progress` 把暂停前的旧 `deadline_at` 写回数据库；
- 旧 deadline 已过期，导致恢复约 21 秒后被服务端判定 `timed_out`。

这不是跳题，也不是 UTC、时区或毫秒换算问题，而是客户端旧 deadline 覆盖服务端权威状态。

### 暂停恢复规则

按最新产品要求，恢复时：

- 必须回到暂停时的原题；
- 重新播放该题 TTS；
- TTS 完成后由服务端重新设置完整 `180 秒`答题时间；
- 不沿用暂停前 deadline，也不使用暂停剩余时间；
- `save_progress` 不能覆盖题号、deadline、answer_started_at 或超时状态；
- 恢复期限到期后保持 `interrupted`，不能无限恢复。

### 补答功能

新增受审计的 `supplement_tasks` 和独立补答 Session：

- 只允许补答缺失或 timed out 题目；
- 已回答题目禁止补答；
- ID110 只允许补 `ability1__0`；
- 补答使用独立 Session 和版本化媒体文件；
- 媒体确认后才回补原 Session；
- 原有答案、媒体、interrupted 状态和审计记录保持不变；
- 重复点击补答不会创建多个有效任务。

### 数据库和测试

补答需要启动兼容式迁移，新增 `supplement_tasks` 表、Session 关联字段和 `answer_started_at`。迁移演练覆盖重复启动、模拟中断恢复、完整性检查和外键检查，均通过。暂停恢复、补答、并发和回归测试最终达到 `790 passed`。

---

## 8. API Provider 和运行环境配置排查

排查过程中发现 Codex 的 `~/.codex/config.toml` 与 AIPQ 项目的 `.env` 是两套独立配置：

- AIPQ 使用 `PQA_LLM_*` 配置；
- Codex 使用 `~/.codex/config.toml` 的 `model_provider` 和 `[model_providers.*]`；
- 曾存在 `lighting` 和指向 `ca.ihainan.me` 的 `OpenAI` Provider；
- Lighting 额度耗尽时，Codex 仍因 `model_provider = "lighting"` 请求 Lighting；
- 后续将活动 Provider 切换为 `OpenAI`，并设置 sub2 地址和单独的 Key 文件，同时保留配置备份；
- 该配置切换不等于 AIPQ 服务的 `.env` 切换，必须分别重启对应进程或重新打开 Codex。

本节只记录配置排查过程，不输出任何 API Key。Lighting 是否彻底从当前 Codex 进程缓存中清除，需要以完全退出并重新打开后的实际请求 URL 为准。

---

## 9. 测试与部署记录

### 主要测试里程碑

本阶段多轮隔离测试逐步扩展，主要结果包括：

| 阶段 | 结果 |
|------|------|
| 生命周期、看板、媒体和暂停修复回归 | 690+ passed，随后持续回归通过 |
| 并发容量和 FFmpeg 单槽 | 8/20/50 请求竞争测试通过 |
| 录音质量、Safari fallback、连续性监测 | 专项通过，随后完整回归通过 |
| 画面采样和 fixture 隔离修复 | `776 passed` |
| 暂停恢复和补答实现 | `783 passed` |
| 迁移演练和补答闭环 | `790 passed` |
| 最新门禁修复 | `796 passed` |

共同通过项目：

- Python `compileall`；
- 全部 JavaScript `node --check`；
- `git diff --check`；
- SQLite `integrity_check` 和 `foreign_key_check`；
- 测试运行时未打开生产数据库、媒体、TTS 或导出路径。

唯一持续出现的 warning 是第三方 `pydub` 导入 Python 3.12 已弃用的 `audioop`，不是本阶段功能错误。

### 已完成的生产发布

- 已执行多次数据库在线备份，备份权限均为 `600`；
- ID266、ID14 恢复媒体已使用版本化名称发布；
- ID249 恢复视频和音频已发布并完成播放、下载和完整性验收；
- 录音质量相关代码曾在安全窗口部署，Web/Worker/Nginx 状态和静态资源校验通过；
- FFmpeg 转码并发配置已在生产确认。

### 当前尚未完成

- 暂停恢复“恢复后原题重新获得完整180秒”及补答功能最新代码尚未完成生产部署；
- 最新兼容式数据库迁移尚未在生产执行；
- ID110 尚未创建正式补答任务；
- ID184 尚未执行生产状态回补；
- Safari 15.1 真实设备验收尚未完成；
- ID261、ID264 的历史无画面视频无法通过软件恢复；
- ID3 历史缺失分段仍未伪造或补齐。

---

## 10. 当前结论和下一步

### 当前结论

已知的代码 Bug 大部分已经定位并在隔离测试中修复，包括：

- 预开始 Session 被错误计入正式答题；
- 正式答题无限并发；
- FFmpeg 并发过高；
- Safari WebM 兼容；
- 摄像头静态无信号画面漏检；
- 完整长录像被错误时长校验拒绝；
- 答案早于媒体确认完成；
- 暂停恢复后旧 deadline 覆盖新计时；
- pending 答案未自动回补；
- 恢复媒体发布后看板仍显示历史失败。

### 下一步

1. 等待或确认所有 `pre_start_active` 页面离开，再进行生产迁移和部署；
2. 备份数据库并执行 `integrity_check`、外键检查和迁移演练对照；
3. 部署暂停恢复和补答代码，重启 Web/Worker；
4. 用测试账号验证恢复原题、TTS 后完整 180 秒和媒体 checkpoint；
5. 安排 Safari 15.1 新 Session 做真实摄像头、麦克风、MP4 fallback 和播放验收；
6. 审计后为 ID110 创建只补第 1 题的补答任务；
7. 继续观察新完成 Session 的视频画面、音频、转码和看板状态。

---

